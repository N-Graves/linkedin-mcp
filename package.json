{
  "name": "linkedin-mcp",
  "version": "1.0.0",
  "description": "Minimal MCP server for LinkedIn's official Posts API (v2 OIDC userinfo + rest/posts) - no scraping, no stealth browser.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "linkedin-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
